# Suhinsmi Pro Website

Upload `index.html`, `styles.css`, and `script.js` to the root of your GitHub repository.

Edit these first:
- Replace `hello@example.com` in `index.html`
- Replace the About text
- Rename Project One, Two, and Three
- Replace each project `href="#"` with your real link

GitHub Pages:
Settings → Pages → Deploy from a branch → main → /(root)
